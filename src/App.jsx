import { useState } from 'react'
import './App.scss'

const App = () => {
	const [count, setCount] = useState( 0 )

	return (
		<div className="Wrapper">
			<main>
			</main>
		</div>
	)
}

export default App
